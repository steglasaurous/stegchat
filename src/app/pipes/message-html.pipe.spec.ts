import { MessageHtmlPipe } from './message-html.pipe';

describe('MessageHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
